let Num1 = 9;

//let Num2 = Math.floor(Num1); // Aredonda para baixo.
//let Num2 = Math.ceil(Num1); // Aredonda para cima.
//let Num2 = Math.round(Num1); // Aredonda para o mais proximo.
//console.log(Math.max(1, 2, 2, 3, 4, 5, 7, 7, 4, 2, 2,22)) // Encontrar o maior numero de uma sequencia
//console.log(Math.min(1, 2, 2, 3, 4, 5, 7, -7, 4, 2, 2,22)) // Encontrar o menor numero de uma sequencia
//console.log(Math.random()) //gerar numero aleatorio
//console.log(Num1 ** 0.5); // raiz quadrada de uma numero.

//console.log(Num2);